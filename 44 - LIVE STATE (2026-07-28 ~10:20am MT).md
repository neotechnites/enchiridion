# 44 - LIVE STATE (2026-07-28 ~10:20am MT) — supersedes 41

> Entry path: [[42 - SPIN-UP (the reusable entry)]] first — this note is STATE, not concept.
> Times below in MT unless suffixed Z. Account: shared prod, ~$300 v4 / ~$100 nestor budget split.

## RUNNING (VPS 129.146.115.241, key ~/.ssh/senate_vps_ed25519)
- **nestor** (`nestor.service`): streak live (10 signals since 6am; volbook window 11:30am
  Mon-Wed; house probe armed). **AUDIT REPAIR RAN ~10:30am (Ryan's order)**: 5 dup settled rows
  removed, bankroll → $95.02, phantom POTUS gone, audit's clean ledger installed (drift entry
  −84.9563, target −17.5229). Divergence Δ$0.00 on the tight $2.00 band, halted=false, trading.
  Residual: v4 velocity still drifts vs the static ledger entry until v5's cash feed (G0)
  replaces it. Resume ritual: stop → `./nestor resume` → start.
- **lip_maker_v4: DEAD, ~11:15am MT, Ryan's order ("turn it off asap"), PERMANENT.** Was
  crash-looping ~29x/hr on `KeyError: 'yes_recon'` (post ~10 orders → die ~2min → cancel-all
  → systemd restart — Ryan saw the churn; operator's "passive" read was a snapshot artifact).
  Killed WITHOUT closing anything (SIGKILL path so its cancel-all never ran; zero orders were
  resting at kill — the loop's own doing). Unit inactive+disabled. 28 positions untouched,
  ride to prints (UST ~1:30pm today; gas tomorrow AM). No maker is quoting anything now;
  capital returns only through v5's gates. v4 postmortem exhibits: crash loop, $10/rung cap
  breached by refill accumulation (98ct/$53 on UST10-4.61), ledger attribution lost
  ($81 claimed vs ~$334 exchange truth), UST orders posted despite deny list.
- Watchers: health_watch→ntfy (Ryan IS subscribed now); recon detect+page every 600s
  (freeze OFF — 3 arming guards documented at the constant).

## v5 — FINISH ROUND IN FLIGHT (~12:30pm MT): first-principles review found the requoting
## stage WAS NEVER WRITTEN — Maker.place() has zero call sites; the assembled system computes
## allocations and drops them. Plus: horizon exclusion inert (close_ts ≡ program end), day
## stop blind (mids unplumbed), drawdown measures deployment not loss, venue ratchet unwired,
## vacuous 0==0 one-path test. Cause: seam-ownership gap + defense-only review frame — now
## doctrine in briefs (affirmative-path audit, aliveness test). Fix chartered:
## work/charter-lip-v5-finish.md, implementor running. Then ONE adversarial round
## (affirmative-first), THEN gates. Prior round's restart blockers folded into the charter.
## v5 (the cutover target) — branch `lip-v5-build`, worktree nestor-wt-lipv5, STAGED-INERT
Spec work/spec-lip-v5.md (final). Build ~390 tests: (★) net-rate with carry term, PSDH toxicity,
verified-accrual ratchet, computed cash feed (6 defects found+fixed pre-live), 13 guard rails
B1-B13 (incl. dual-measure cluster cap — this morning's correlated-treasury book is a refusing
fixture), G0 nestor-reader patch (reviewed, machine-applyable, unapplied). REMAINING: outer run
loop + scanner sweep (in flight), then ONE final adversarial round of the assembled system, then
Ryan's staged cutover (README 10 gates; --allow-fresh explicit; cutover triage exits Ryan-authorized;
crossing exits stay gated). DO NOT deploy any v5 gate without the final round's verdict.

## MONEY (audit work/audit-2026-07-28.md — identities close to ~1¢)
Cumulative LIP op +$63.79 mid / +$33.30 ex-variance; rewards $7.482 PAID+verified (exchange
estimates proven honest; per-rung line items in Credits). Today intraday negative (Ryan's
440→377): treasury correlated-bet run-over (−$60 class, the B1 incident) + hourly toxic fills
(−$20, since banned). Measured constants (decaying): reward ≈0.36%/hr on deployed; round-trip
exit cost 5.4¢/contract ≈12.9% notional; instant-flatten REFUTED (work/backtest-instant-flatten.md);
crossover ≈56h ⇒ pay-to-exit only for long-horizon inventory.

## PENDING / NEXT
1. **Ryan paste**: audit_repair (above). 2. v5 final round → cutover gates (Ryan's word each).
3. UST settlement ~1:30pm + gas 26JUL29 tomorrow AM — settlements feed the credits ritual
   (Credits page → recon.jsonl; 22:00Z reminder alert armed in v5). 4. Land-grab validates at
   tonight's listings (~8pm) / tomorrow 6am opens. 5. Wednesday: $1k decision on the prints;
   FOMC strangle gate 12:40pm (manual, non-LIP). 6. Subaccount (R153) = top funding gate.
7. Fold-in list for v5 (Ryan reviews): settlement-print-in-covered-band entry test, A-S size-skew.

## Standing constraints (see briefs/ + note 23 III)
Freeze on v4 (Ryan's word only). No hand-authored ledger rows. Staged calls for human-gated
money decisions. MT times to Ryan. One state writer. The R-log is evidence — grep, don't read.
