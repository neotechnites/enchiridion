# 39 - LIVE STATE (2026-07-26 evening) — spin-up entry point (supersedes 36)

> **Read order:** this file → [[21 - The Senate Charter (2026-07-23)]] (the ONE goal — read first,
> always) → [[23 - Derivation First (the spin-up ritual)]] (PERFORM Part I; Part II binds every
> spec→code translation) → [[38 - The Strategy Book (living inventory)]] (Ryan's four-column
> pipeline = THE frame; evidence classes = lifetime not confidence) → [[24 - The Cycle]] →
> [[19 - Senate Architecture]] → [[33 - The Mesh]] → [[15 - Operating Manual]] (Ryan's rules —
> every one was learned through his anger; violating them re-earns it) → [[17 - Conversation Log
> (Session 3)]] **R73-R158 = the entire story. Read the R-entries before acting on anything.**
> Ideator system when needed: 32/34/20/35. Then act — never re-derive, re-ask, or re-kill.

## WHERE THINGS STAND (Sun 2026-07-26 ~17:40Z)
- **NESTOR LIVE, one process, THREE strategies + ws shadow** (`~/Documents/senate/nestor`,
  `./target/release/nestor run`, .env: NESTOR_ENV=live, VOLBOOK_LIVE=1, HOUSE_PROBE=1).
  **Bankroll $106.03** (first win +$6.03 banked Fri; no losses yet — never taken one live).
  1. **STREAK**: signal = 4-streak + reversal ask ≤44¢; order = IOC **limit 46¢** (R155/R156:
     ceiling ≠ gate; EV +7.0¢ measured / +4.3¢ conservative) with 4 attempts × 2s (-r{n} coids),
     per-retry book snapshots (diagnostic, R154), derive-fourth (6/6 verified, 3 used). Live
     record: 6 signals / 1 fill / 1 win — 2 misses were the fixed bid-the-ask bug, 3 were
     boundary-44 phantoms/staleness → the 46 ceiling + websocket answer them.
  2. **VOLBOOK** (metal wings Mon-Wed, T-3h, per-bucket willingness-to-pay ceilings): armed,
     FIRST live window Mon ~lunchtime ET — trades automatically if rungs qualify (a no-trade
     Monday = gates working, not malfunction). Sizing $4/rung, day-cluster ≤15% bankroll.
  3. **HOUSE probe** (H10 econ + H9 political makers): 1-lot two-sided quotes when spread ≥2¢,
     −$20 in-code sticky stop, catalyst pulls. Zero quotes yet (weekend spreads 1¢ = gate
     working). 2-3 day probe → promote/kill per work/probe-house.md metrics via `house-report`.
  - **WEBSOCKET (shadow)**: prod-connected, seq-per-sid fix live, book ages 2-88ms vs REST's
    500-3000ms staleness (R156 anatomy). STREAK_WS=1 flips ONLY after data/ws_divergence.jsonl
    proves the win. A dead ws never blocks entries — REST is the floor.
- **LANDED: the fitted execution policy (work/verify-streak-execution.md — IMPLEMENT THIS,
  it supersedes the current taker-only policy AND the R157 ladder sketch).** Cont-Kukanov frame
  fitted to our churn-filtered 100ms tape (174 windows/coin): reversal side opens median 53¢,
  dips to median-min 44-47¢ bottoming ~5s, then sweeps UP 55% of the time — cheap fills are
  transient, so REST BEFORE THE BOUNDARY. Policy: at T0−5-10s (derivation decisive) post a
  resting GTD BUY, full size, at **40¢**; if unfilled by ~T0+45s, IOC taker backstop at ceiling
  **48¢**; ask >48 all window = correctly no trade. E[capture]/signal: +30-40% vs current
  (2.5→3.6¢ conservative, 4.9→6.2¢ at measured win rate). **Fable review rulings:** rest-at-40
  = accept as-is (no new population, strictly better). The 48 backstop trades a NEW population
  (45-48 windows the 44-gate never traded) and the researcher's own flagged risk lives exactly
  there (conditional win rate in swept/never-dipped windows unproven) → **ship the backstop at
  46 first** (keeps the 2.5×-fee cushion; still captures most discarded windows), walk to 48
  only when live fills at 45-46 prove the conditional win rate. Maker-entry fills get tagged
  and tracked separately. Implementation = the FIRST build task (machinery exists: maker module
  + derive margin + cancel; ws book makes cancel-on-flip fast).
- **Machines**: 9 athena daemons + listing fast-snap (60s first-2h). Watchers: health_watch.sh
  (10min launchd) + AI reviewer (opus/medium 2×/day, ~/kalshi_data/review/) — BOTH partially
  BLIND on the Mac: launchd+TCC blocks ~/Documents reads (R154; explains all false alarms).
  Dies with the VPS (Linux, no TCC).

## THE IMMEDIATE WORLD (what the next Claude walks into)
1. **VPS migration tonight with Ryan** — work/vps-runbook.md (VERIFIED tiers: Oracle Always
   Free CUT to 2 OCPU/12GB but ample, PAYG conversion MANDATORY, us-ashburn-1, retry capacity;
   fallback = AWS $100-200 credits ~6mo; Hetzner DOUBLED, dropped). Latency dividend measurable
   (order RTT 170-300ms → 20-60ms). Cutover ordering matters (phase 4).
2. **Monday**: three firsts at once — volbook's first window, house's first quotable spreads,
   streak under the 46 ceiling. Watch all three tapes; the reconstruction discipline (15) applies.
3. **Tuesday**: FEDMENTION downgraded (Warsh regime, market efficient — R135); capture runs,
   builds Warsh base rates for NEXT meeting. **Wednesday**: PCE/GDP index wings + MSFT/META gap
   wings (Ryan's slate) — decide build-vs-manual before then.
4. **Weekly Fable review ~Jul 31** — duties in 38 (incl. backtest-lock re-scan, weather maturity
   check, decay-bench sweep). Weather forward-capture matures ~mid-Aug.
5. **Maker-system separation + LIP** (R153): post-probe, separate binary + Kalshi subaccount +
   sleeve; LIP pays $10-1000/day for resting size 100-20k contracts, ends Sept 1 2026.
6. **Week-2 streak sizing**: flat $4 until 8 clean fills under the current policy → 5% fraction
   (R148). **Seed-prior**: gate #1 closes passively via fast-snap (top of column 3; model built,
   +26.4¢ net validated, ~/kalshi_data/seed_prior.json).

## OPS GOTCHAS (Mac-era; several die with the VPS)
Restart: `pkill -f "nestor run"` then `cd ~/Documents/senate/nestor && (nohup ./target/release/nestor run >> logs/run.log 2>&1 &)` — the permission classifier BLOCKS kill intermittently; retry, it's flaky, third try tends to work · one state-writer lock (NESTOR_STATE_PATH override for side runs) · demo creds: SECRETS.local.md (untracked) + secrets/Demo.txt PEM; demo probes = the pre-prod ritual, they catch what docs get wrong (they've caught 4 API truths so far) · subagents: opus researcher/researcher-med ONLY (never Fable) · token doctrine R132: no scheduled Fable turns past cache TTL · stalled workers → SendMessage "finish synchronously" · API truths: only *_dollars/_fp fields live · REST lags the engine 0.5-3s in bursts · resting-orders list is eventually-consistent (responses are truth) · order expiration enforced lazily (~2-3min sweep) · ws: seq per-sid across ALL frames incl. ok-frames; snapshot keys are *_dollars_fp · duplicate coid → 409 (retries use -r{n}).
