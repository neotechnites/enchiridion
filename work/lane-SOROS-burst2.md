# Lane SOROS — burst 2 (2026-07-24)

Archetype: SOROS (reflexivity) — thin books that gap on their own momentum, herding overshoot, self-referential settle.
Steer (burst 2): (a) TEST S3 now — Kalshi trade prints for political tickers, measure post-spike overshoot-fade (disk-testable). (b) SPEC the Poly→Kalshi politics-lag watcher precisely. Both done. No scout/spawn tool surfaced (as burst 1) — SendMessage-only, no reachable researcher; ran disk kills synchronously, nothing waited on monitors.

## Headline result
**S3 (news-spike overshoot-fade in a thin political ladder) is DEAD — a bid-ask-bounce artifact.** The reversion is real on traded-price MARKS (+1.72c/day, day-clustered, t=4.56, 77% of days, both eras) but a taker capturing it must enter at the spike bar's last-traded price (the far side of the spread). Enter even 2 minutes later at a real available price and the entire edge vanishes: +0.17c (t=0.98). Horizon-invariant snapback (present at 10min, doesn't grow to 120min) = bounce, not underlying mean-reversion. Gate-hunt exhausted: ≥8c and ≥12c spike slices also collapse under the 2-min delay. This extends the graveyard's spike-fade/bounce doctrine to POLITICS and REDIRECTS the archetype: intra-venue behavioral overshoot-fade is not a taker door in any family. The surviving reflexivity door is CROSS-VENUE (S2), spec'd this burst.

## Ledger

| # | idea | mechanism / fish | kill-test | numbers (n · result · split) | verdict | files |
|---|------|------------------|-----------|------------------------------|---------|-------|
| S3 | Kalshi news-spike overshoot-fade in thin political ladder | Breaking poll/news → retail momentum stampede overshoots a thin Kalshi approval ladder past fair; fades over 15-60min. Fish = news-herding retail; MM vacuum lets the print gap. Kalshi KXAPRPOTUS (Trump-approval point ladders) | DISK: `appr_trades_all.json.gz` (72 tickers, 62.7k trades, May16-Jul17). 1-min bars; spike=net \|Δ\|≥5c over 10min; measure next-30min fade; PLACEBO=flat windows; then (decisive) ENTRY-DELAY 0/2/5min to separate genuine reversion from spread-bounce; day-cluster to kill correlated-sample inflation; era split | Raw fade EV +1.99c (t=11.8), placebo clean (+0.03c t=0.9). Day-clustered n=52 days +1.72c t=4.56, 77% pos; split May-Jun +1.54c(t=5.2)/Jul +2.20c(t=1.9) both same dir. **ENTRY-DELAY KILL: DELAY=2min → +0.17c (t=0.98); DELAY=5 → +0.22c(t=1.33). Gate: spike≥8c DELAY=2 +0.49c(t=1.31); ≥12c DELAY=2 +0.67c(t=1.25) — all insignificant.** Horizon flat (10→120min: +1.51→+1.77c, no growth) | **DEAD** (structural: bid-ask-bounce artifact; reversion is spread, not capturable by a taker at any real entry price. Gate-hunt exhausted.) | `~/kalshi_data/scripts/soros_s3_overshoot.py`, `soros_s3_cluster.py`, `soros_s3_econ.py`, `soros_s3_gate.py` |
| S2 | Cross-venue Poly→Kalshi POLITICS lag | Political news reprices DEEP Poly book first; THIN Kalshi political ladder follows minutes later — Poly's move is a leading signal, Kalshi gap-closes same-direction. Fish = inattentive Kalshi political quoters. Reflexive, minutes not ms. TRADE Kalshi (Poly=signal, charter). Distinct physics from S3: rides the LAGGING venue toward the LEADER (continuation), not a same-venue snapback | OFF-DISK capture. FIRST cheap probe (≤10min, no daemon): do ≥5 matched Kalshi↔Poly-US political pairs with two-sided OI>500 books exist? If yes → build `polylag_watch.py`; if <5 → DECAY-BENCH. Then forward: log gap≥4c episodes, track Kalshi gap-closing over 15min, placebo=no-gap windows, cluster by EVENT | not run (no scout/spawn tool; SPEC'D as build ticket) | **CONDITIONAL-needs-capture** (gate: Poly is deeper/attention-leading book AND Kalshi political ladder thin; must clear buildability probe first). The single most-promising surviving SOROS door | `work/build-polylag.md` (full daemon spec, reuses xvenue_watch patterns) |
| S1b | Thin-ladder overshoot capture as MAKER (not taker) | S1(burst1)+S3 collide: thin non-crypto books DO overshoot and revert on the mark (+1.7-1.9c), but the taker side is now PROVEN dead (S3 entry-delay). The reversion IS the spread → a MAKER resting on the far side of a spike captures exactly it. This is the taker-only doctrine's known complement (MM-absent books). Fish = momentum-herding wing/ladder buyers | Requires live-fill capture in these books (can't sim maker fills from trade prints). CONVERGES with already-queued A6/A3 commodity-book capture (note 32 §5) — now +1 vote from politics too | S3 provides the mechanism proof (spike reverts, spikes are noise not info) but maker fill-rate unmeasurable on disk | **CONDITIONAL-needs-capture** (gate: MM-absent thin book + spike = noise; verified only by live maker fills. Note: maker=pickoff doctrine is the counter-risk — capture must measure pickoff vs benign-drift, per HOUSE burst-2 steer) | — (folds into A6 capture demand) |
| S4 | Favorite-herding overshoot in RACE ladders | Recency/bandwagon bettors pile into a favorite the days AFTER a catalyst, overshooting; reverts. Fish = recency-biased bettors. Kalshi KXMIDTERMMOV/KXHOUSERACE/sports-WINS | OFF-DISK multi-day ladder snapshots; event-study favorite price post-big-day vs settle; placebo=flat days | not run — BUT S3 supplies a bearish prior: this is the SAME physics (thin-ladder behavioral overshoot) that S3 just proved is a taker-uncapturable bounce. Downgraded from burst 1 | **CONDITIONAL-needs-capture** (bearish prior: likely inherits S3's bounce fate; only worth building if the daily-bar (not intra-session) horizon changes the microstructure. Low priority) | — |
| S5 | Poll-release info-lag on the approval ladder (NEW) | The approval ladder's SETTLE SOURCE (538/RCP/Silver-Bulletin poll aggregate) updates when new polls drop on a semi-known schedule; the thin ladder lags the released number by minutes-hours (mention-ratchet shape: official source moves, crowd trades vibes). Buy the ladder in the direction of a just-released poll before the crowd. Fish = ladder quoters who haven't read the new poll. Kalshi KXAPRPOTUS | OFF-DISK: needs poll-release TIMESTAMPS matched to ladder price paths (info-channel capture). Not the bounce door — this is a REAL information event with a directional edge, not a mean-revert | not run (needs poll-release-time feed; genuinely new door surfaced by S3's data) | **CONDITIONAL-needs-data** (gate: known poll-release cadence + measurable ladder lag; distinct from S3 because the settle source genuinely moved. Scout candidate: which aggregator settles KXAPRPOTUS + its update cadence) | — |
| S8 | Pure outcome-reflexivity (betting moves the real settle) | Purest Soros door: depegs/bank-runs/resign-under-pressure where our betting changes the outcome | DISK census `catalog_open_202607.json` (burst 1) | Zero venue: reflexive-sounding families all fundamentals-driven binaries; no depeg/bank-run market listed | **DEAD** (structural, cited from burst 1 — not re-run) | — |

## Pipeline note (synthesis / capture queue, note 32 §5)
- **The archetype's core shape (behavioral overshoot in thin books) is now PROVEN taker-dead intra-venue** (S3, decisively, with the entry-delay discriminator). Do not propose more intra-venue spike/momentum/round-number fades in any family — they re-derive S3's bounce. This is a hard redirect, on par with burst-1's "leave crypto momentum."
- **Two surviving doors, both cross-mechanism, both capture-gated:**
  1. **S2 Poly→Kalshi politics lag** — the top SOROS build candidate. Spec complete (`work/build-polylag.md`). Needs the ≤10-min matched-pair buildability probe FIRST (cheap, decides go/no-go), then a forward daemon reusing xvenue_watch. Scout-able the moment a spawn tool exists.
  2. **S1b thin-ladder overshoot as MAKER** — folds into and REINFORCES the already-queued A6/A3 commodity-book live-fill capture (now voted by S1 + S3-mechanism + this). One capture verifies A6/A3/S1b together. Counter-risk = maker pickoff (reconcile with HOUSE burst-2 adversarial test).
- **New scout target surfaced:** S5 poll-release cadence for KXAPRPOTUS (which aggregator settles it, update schedule) — turns a dead bounce family into a possible directional info-lag door.
- **Zero re-litigation:** crypto momentum/streak/velocity/volume all cited from graveyard; S8 cited from burst 1; only S3 consumed compute — one decisive kill, fully powered.
