# Lane THORP — Burst 1 (2026-07-24)

Archetype: **THORP (thin-capacity)** — structural mispricings a $1k bankroll can OWN because size can't bother: obscure series, low-volume ladders, markets too small for pros.

**Spin-up done:** notes 18 → 21 → 23(ritual performed) → 20 → 32 → 34 → 33 (Mesh, complete). Graveyard grepped (13, archive/03, night 27-31) + peer lanes MUSK/BUFFETT burst-1 + steer-daytime.

**Ritual descent:** goal=power→money→nestor max on Kalshi. State: peers (MUSK census, BUFFETT certainty) both concluded "liquidity = known slate; untouched categories dormant." THORP's job = find the thin LIQUID ladder with a *structural* (non-info) mispricing size ignores. Maximize: a THORP edge must be a **free structural lock** (over/under-round basket, cross-rung dutch, crossed book) on a ladder too thin for pros to bother. Therefore: the cheapest decisive kill is a **single live snapshot** across every liquid multi-rung ladder — that snapshot IS the synchronized-quote capture BUFFETT (B9) lacked. Run it once, kill four lock-types at once.

**No subagent-spawn tool exposed in this workflow** (confirmed by both peer lanes; only SendMessage to a non-existent researcher). Ran all reconnaissance myself via keyless Kalshi API — cheaper and controlled.

**KEY API QUIRK (nail it):** on `/events?with_nested_markets` and `/markets`, the plain fields (`yes_ask`,`open_interest`,`volume`) read **None for every market**. Live data lives ONLY in `*_dollars` (string prices), `open_interest_fp`, `volume_fp`, `yes_ask_size_fp`, `yes_bid_size_fp`. NO-side size ≈ `yes_bid_size_fp` (buying NO@no_ask = hitting yes_bid). Peers half-knew this; documenting fully.

---

## The decisive run — `scripts/thorp_basket_scan.py` (+ `thorp_detail.py`)
One snapshot, paginated 40 pages → **4,740 liquid multi-rung events** (≥2 rungs with OI>0 + takeable ask): **2,329 mutually-exclusive (ME) partitions + 1,048 monotone `greater` ladders + 1,363 other.** Four TAKER lock-types tested with takeable-size checks:

| type | mechanism | hits / n | on inspection |
|---|---|---|---|
| (A) under-round buy-all-YES partition (Σyes_ask+fee<100) | one outcome pays $1, basket costs <$1 | 36 / 2329 ME | **all NON-EXHAUSTIVE** — Σyes_bid≈0-17. NFL-next-team 32×1¢, KU-coach 25 names, CPIYOY 16 "exactly X%" points: low Σ = real *unlisted residual* outcome prob, not arb. Basket loses when residual hits. |
| (B) over-round buy-all-NO partition (Σno_ask+fee<(n−1)·100) | N−1 NOs pay $1 | 1 / 2329 | KXHOUSERACE-IL04 @0.52¢ = fee-noise on a near-exhaustive 3-way (R/D/other) → efficient |
| (C) monotone nested-ladder dutch: buy YES(K1)+NO(K2), K1<K2, cost<100 | ≥K1 ∪ <K2 always true | 148 / 1048 | **all mis-paired same-strike sports legs** (home/away spread lines, not nested). True nested ladders (NBAWINS-27WAS, MIDTERMMOV-WYSENR) verified **monotone-coherent, spreads 1-20¢ → no taker lock** |
| (D) crossed single book (yes_ask+no_ask+fee<100) | buy both, one pays $1 | **0 / 4740** | none exist |

**Structural conclusion:** thin categorical ladders are EITHER non-exhaustive + one-sided (the "which-X" longshot books: bids≈0, asks only — MMs sell you lottery tickets, quote no bid) OR monotone-coherent (the nested win-total / margin ladders, two-sided but spread-wide). Neither leaves a free structural lock. **Gate-hunt performed:** the only rescuing gate = "exhaustive AND two-sided AND thin" partition; empirically empty — where a partition tiles the outcome space it is either the liquid index/crypto/commodity daily (peer-covered: tight / stale-unfillable) or the near-exhaustive election R-vs-D (efficient, fee-noise). Kill is STRUCTURAL (low Σ = genuine residual prob; one-sidedness = no counterparty), not merely conditional → DEAD is legal.

---

## Ledger

| id | idea | mechanism / fish | kill-test | numbers | verdict | files |
|---|---|---|---|---|---|---|
| T1 | Under-round buy-all-YES basket lock on thin "which-X" ME ladders (primaries, next-coach/team, awards) | Fish = retail lottery-buyers paying the ask on longshot outcomes; absent MM quotes no bid. Thesis: Σ of listed longshots < $1 → buy all, one pays $1 | live snapshot: Σyes_ask+fee vs 100 on EXHAUSTIVE ME partitions w/ takeable size | 36 apparent hits / 2329 ME events; **all non-exhaustive** (Σyes_bid 0-17, unlisted residual carries the missing prob) — e.g. NFL-next-team 32×1¢=32, KU-coach Σ47/Σbid17, CPIYOY Σ74/Σbid1 | **DEAD (structural)** — `mutually_exclusive=True` ≠ collectively exhaustive; low Σ is real residual-outcome prob, basket loses on the unlisted outcome | thorp_basket_scan.py, thorp_detail.py, /tmp/thorp_basket.json |
| T2 | Over-round buy-all-NO basket harvest (BUFFETT B9's door, now with the synchronized snapshot) | Fish = absent MM never enforces Σprob=1; sell the whole disjoint basket when it over-rounds | Σno_ask+fee vs (n−1)·100 on ME partitions | 1 hit / 2329 (KXHOUSERACE-IL04, +0.52¢) = fee-noise; thin ladders are **one-sided** (Σyes_bid≈0 → no_ask≈100, buying NO costs ~$1 each) | **DEAD (structural)** — resolves BUFFETT B9: the over-round harvest needs two-sided books; thin categorical books quote asks only. Only near-exhaustive 2-3-way (elections) are two-sided and they're efficient | same |
| T3 | Crossed complementary single book (yes_ask+no_ask<100) on thin binaries | Fish = a lagging one-sided quote crossing its own complement | scan every live market's yes_ask+no_ask+fee vs 100 | **0 / 4740 events** | **DEAD (structural)** — single-market books never cross net of fee; internal complement is arbed | same |
| T4 | Monotone nested-ladder cross-rung dutch on thin non-crypto ladders (win-totals, election margins, COUNT) | Fish = a stale rung in a nested `≥K` ladder; buy YES(low K)+NO(high K) < $1 | pair strictly-increasing floor_strike legs, cost<100 w/ takeable size; verify true ladders NBAWINS/MIDTERMMOV | 148 scan hits = **mis-paired same-strike sports spread legs** (artifact); true nested ladders **monotone-coherent, spreads 1-20¢**, no taker lock; COUNT family efficient (MUSK B2) | **DEAD (structural)** — nested ladders internally coherent; extends crypto/index dutch-death (note 13, MUSK M3) to sports/election/count nested ladders with numbers | same |
| T5 | Discrete "Exactly X%" econ point-ladder overpricing (KXECONSTATCPIYOY-style) | Uniform ~4¢ ask across 16 "Exactly 2.1%…3.5%" points ignores that realized CPI concentrates on 1-2 points → far points overpriced. Fish = retail exact-value lottery buyers at 4¢ | realized CPI-YoY point distribution vs the 4¢ uniform ask, per-point EV | on-disk: only a live snapshot (Σyes_ask=74 across 16 points, one-sided, bids≈0). No realized-point history on disk | **CONDITIONAL-research** — genuine discrete-settle structural class; SELL side is maker/one-sided (blocker) + points sit at 4¢ (edge of "1-3¢ fair" tail). Needs realized-print history + a maker-fill probe. Distinct from wing-sell (point-mass, not tail band) | live API |
| T6 | The thin-LIQUID frontier is large — map it (census correction) | Recon: is peers' "liquidity=known slate, categories dormant" true? | count liquid two-sided multi-rung ladders outside the slate | **2,159 liquid multi-rung event-families** outside the slate: KXMIDTERMMOV (322), KXHOUSERACE (349), NBA/MLB/NFL/WNBA/NCAAF WINS ladders, KXBOXING, KXRT (Rotten Tomatoes), KXALBUMEQUIV/ARTISTSTREAMS, KXNEXTTEAM/COACH — OI 100-100k, two-sided | **DEAD as a structural door / OPEN as an info door** — corrects the Mesh: the thin-liquid frontier is real & vast (THORP's native habitat), but structural locks are coherent (T1-T4) → the edge here is INFORMATIONAL (model the sport/election/award better). **Routed to BENTER / INFO-CHANNEL**, out of THORP's structural remit | /tmp/thorp_basket.json (fams) |
| T7 | Elimination-residual on nested win-total rungs (computable-class transfer) | Once a team is mathematically eliminated from ≥K wins (games_left < K−current_wins), YES(≥K)=0 but retail may leave a takeable ask >0. Fish = crowd not updating games-left arithmetic | live nested-ladder rungs where math-locked-dead still shows yes_ask>1¢ with size | untestable on disk (needs standings + games-left feed; NBAWINS is 2027-future=0 games played, MLB in-season). BUFFETT B8 found *top-level* season totals "1.00 at clinch, zero volume" — mid-ladder rungs unchecked | **CONDITIONAL-research** — needs a live standings/games-left feed; the fade (not buy) side + mid-ladder rungs are the unexplored angle. Capacity-tiny = THORP-suited | — |
| T8 | New-listing 48h sloppiness on thin categorical ladders (Mesh open door) | First 48h of a new nested/partition ladder before bots arrive: wider spreads, mispriced rungs a $1k can pick off. Fish = the not-yet-present MM | capture listing timestamps vs spread/edge decay over first 48h | untestable on disk (needs a listing-timestamp monitor) | **CONDITIONAL-research** — matches MUSK's wake-up/new-listing monitor capture demand; the listing monitor is a strategy-generator (Mesh). THORP-capacity by construction | — |

## Summary
- **8 ideas. 0 TRADE, 0 PROMISING.** 4 DEAD-structural (T1-T4, the basket-lock cluster killed in one 4,740-event snapshot), 1 census-correction (T6: DEAD structural / routed to info-lane), 3 CONDITIONAL-research (T5 discrete-point overpricing, T7 elimination-residual, T8 new-listing sloppiness — all feed-gated).
- **The decisive numeric finding:** across 4,740 liquid multi-rung ladders, **zero free structural locks** — thin categorical books are non-exhaustive+one-sided (which-X longshots) or monotone-coherent (nested ladders). The THORP *structural* door on thin categoricals is closed, with numbers. This closes BUFFETT B9 (over-round needs two-sided books that don't exist thin) and extends the ladder-dutch death (note 13 crypto/index, MUSK M3) to sports/election/count/award nested ladders.
- **The corrective finding (Mesh update):** peers' "liquidity = known slate, untouched categories dormant" is **too pessimistic** — there are 2,159 liquid two-sided thin multi-rung families outside the slate. They carry no structural edge, but they are the native habitat for a **BENTER feature-model** (model NBA win-totals / senate margins / RT scores better than a lazy MM) — the strongest *routed* door out of this lane.
- **Capture demand surfaced (note 32 §5):** (1) realized-print history for discrete "Exactly X%" econ point-ladders (T5); (2) a live standings/games-left feed for elimination-residual (T7); (3) a new-listing timestamp monitor over the 2,159-family catalog (T8 + MUSK M1/M6 wake-up + Mesh "first 48h") — one monitor serves three lanes.
