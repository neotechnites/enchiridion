# STEER: Ideation burst 2026-07-27 (Ryan-ordered, 2% weekly ≈ 2.6M; burst 1 = 6 lanes)

> Every lane: read [[33 - The Mesh]] COMPLETELY first (it is the input that makes ideas),
> then your archetype brief in [[34]], then [[20 - Batch Playbook]] discipline + the
> graveyard (Mesh §graveyard + note 15 traps). 8 ideas per lane, mechanism-first (WHO is
> the fish and WHY), cheapest decisive kill for each run inline on ~/kalshi_data (Mac).
> Verdicts: TRADE-shaped / CONDITIONAL(named gate) / DEAD-with-numbers. Ledger to
> work/lane-{ARCHETYPE}-jul27.md, commit no push. Budget ~220k/lane; ≤1 researcher-med
> scout if a foreign fetch is genuinely needed, else everything inline. No nestor changes.

## NEW COLLISION FUEL since the last ideation night (use it — it's why this burst exists)
- **Maker fills billed $0.00** (demo-proven; prod self-confirms on first resting fill).
  The fee quadratic that killed a dozen ideas is HALF-GONE on the maker side.
- **LIP pays $10-1000/day** for resting size 100-20k contracts, ends Sept 1 (R152/153).
- **Markets exist pre-T0 in status "initialized"** and are directly fetchable by
  constructed ticker while invisible in the status=open list until T0+6-36s (probe-proven
  yesterday). Who else is blind to the first ~30s? What's mispriced by whoever isn't?
- **The calibration map** (verify-streak-conditioning + midwindow-fv): Kalshi 15m crypto
  is calibrated EVERYWHERE except the post-streak open. Cheap-side buying dead, mid-window
  models dead, marks calibrated-to-cheap. Stop proposing ideas those ledgers already kill.
- **Fills carry is_taker + fee_cost; coid dedupe outlives orders; cancel reduced_by is
  synchronous truth** — execution-mechanics ideas can now be designed against measured API.
- **2yr regime map**: streak rates BELOW iid in every regime (crypto 15m mean-reverts
  slightly); no macro-regime gate exists on the fade.

## Lanes (burst 1)
1. **HOUSE-FEE** — the maker-fee asymmetry, industrialized: where does "we pay ~0, takers
   pay 1-1.75¢" alone create edge (quote inside spreads that are only profitable BECAUSE
   our side is free), and what does the LIP add on top? Hunt beyond the two probe books.
2. **VENUE-MECHANICS** — the pre-T0 "initialized" window + listing lag + eventually-
   consistent list indexes as a CLASS: what else is visible-to-the-diligent before it's
   visible-to-the-lazy? (Fine print: check who can ORDER during initialized status —
   propose the demo probe, don't assume.)
3. **MUSK** — census refresh: what series families are new/changed since the Jul 24 census
   (12,179 baseline in listing_monitor)? New listings, new fee schedules, new rules pages.
   The COUNT-family playbook applied to whatever appeared since.
4. **BENTER** — computable-trajectory forecast doors: public settle source + lazy crowd +
   fittable model (the measles shape). Sweep the liquid-ladder catalog for fat-tailed
   crowd fear vs computable base rates. The calibration map says this is where residual
   edge must live: where there's NO anchor.
5. **SOROS/EVENT** — Wednesday's cluster (PCE/GDP + MSFT/META) and the FOMC-adjacent
   calendar: event-day structure ideas that COMPOSE with the wings build (tomorrow),
   not duplicate it. What does the wings evidence imply about OTHER scheduled prints
   (jobless claims, EIA, NFP Aug 7)?
6. **BEZOS-TAPE** — depth-mine OUR OWN participation record (streak_week1.jsonl,
   settlements, house_pass, obs, ws_divergence — data nobody else owns): entry-price-
   bucket win rates (Ryan's floor question, per-coin), maker-vs-backstop fill quality,
   time-of-day, per-coin asymmetries, ws-vs-REST edge moments. Small n honesty mandatory
   (label everything; no verdicts beyond the n).

Burst 2 (after Fable steering): lanes re-aimed at burst-1's best doors + a synthesis judge.
